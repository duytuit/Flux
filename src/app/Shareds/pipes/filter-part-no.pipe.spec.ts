import { FilterPartNoPipe } from './filter-part-no.pipe';

describe('FilterPartNoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPartNoPipe();
    expect(pipe).toBeTruthy();
  });
});
