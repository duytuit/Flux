import { FilterLotNoPipe } from './filter-lot-no.pipe';

describe('FilterLotNoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLotNoPipe();
    expect(pipe).toBeTruthy();
  });
});
