import { DetailDirective } from './detail.directive';

describe('DetailDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailDirective();
    expect(directive).toBeTruthy();
  });
});
